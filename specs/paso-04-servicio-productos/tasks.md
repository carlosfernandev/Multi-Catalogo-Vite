# Tareas — Paso 04

> Tareas pequeñas y atómicas: una responsabilidad por tarea, en orden de ejecución. Marcar `- [x]` al completar.

- [x] **T1**: Crear carpeta `frontend/src/components/Catalogo/services/` — *Completado cuando:* la carpeta existe.
- [x] **T2**: Crear `frontend/src/components/Catalogo/services/productosService.ts` con `getProductos` — *Completado cuando:* retorna `Promise` con `setTimeout 400 ms`.
- [x] **T3**: Agregar `getProductoById` con `find` y `setTimeout 300 ms` — *Completado cuando:* retorna el producto o `undefined`.
- [x] **T4**: Verificar imports y compilar — *Completado cuando:* `npm run build` pasa sin errores.
