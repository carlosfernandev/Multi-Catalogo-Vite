# Tareas — Paso 12

> Tareas pequeñas y atómicas: una responsabilidad por tarea, en orden de ejecución. Marcar `- [x]` al completar.

- [x] **T1**: Definir `NIVEL_ESTILO` y componente recursivo `NodoReferido({ nodo })` en `frontend/src/components/MiRed.tsx` — *Completado cuando:* renderiza nodo + hijos con indentación.
- [x] **T2**: Calcular KPIs con `contarRed`, `sumarVentasRed`, `sumarComisiones`, `nivelAlcanzado` — *Completado cuando:* resumen muestra 7 / $4 090 / $319.50 / Plata.
- [x] **T3**: Renderizar raíz "Tú" y árbol con `redInicial.hijos.map(NodoReferido)` — *Completado cuando:* jerarquía de 3 niveles visible.
- [ ] **T4**: Verificar como admin — *Completado cuando:* `/mi-red` muestra árbol completo y comisiones por nivel.
