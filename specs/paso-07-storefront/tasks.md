# Tareas — Paso 07

> Tareas pequeñas y atómicas: una responsabilidad por tarea, en orden de ejecución. Marcar `- [x]` al completar.

- [x] **T1**: Crear `frontend/src/components/Storefront.tsx` con imports de `getProductos`, `categorias`, `useAuth` — *Completado cuando:* el archivo compila.
- [x] **T2**: Implementar hero `min-h-screen` con gradiente y CTAs — *Completado cuando:* ocupa viewport y tiene dos botones (segundo condicional por rol).
- [x] **T3**: Agregar sección categorías (grid 5) con `Link ?categoria=` — *Completado cuando:* cada categoría enlaza a `/catalogo?categoria=...`.
- [x] **T4**: Agregar sección destacados (`getProductos().slice(0,4)`) y "Cómo funciona" + CTA final — *Completado cuando:* se ven 4 cards y 3 pasos.
- [ ] **T5**: Probar roles y viewport — *Completado cuando:* cliente no ve CTA admin y hero cubre pantalla.
