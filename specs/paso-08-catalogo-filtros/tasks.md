# Tareas — Paso 08

> Tareas pequeñas y atómicas: una responsabilidad por tarea, en orden de ejecución. Marcar `- [x]` al completar.

- [x] **T1**: Reemplazar `frontend/src/components/Catalogo/Catalogo.tsx` con `useSearchParams` y estados `busqueda`/`categoria` — *Completado cuando:* lee `?categoria` inicial.
- [x] **T2**: Implementar `useMemo` de filtrado por texto y categoría — *Completado cuando:* filtra sin recalcular en cada render.
- [x] **T3**: Agregar barra de búsqueda, selector de categorías y sincronización de URL — *Completado cuando:* cambiar categoría actualiza `searchParams`.
- [x] **T4**: Implementar grilla, estado vacío y `Limpiar filtros` — *Completado cuando:* sin resultados muestra mensaje y botón.
- [ ] **T5**: Probar filtros y URL compartible — *Completado cuando:* buscar "serum" y `?categoria=Serum` funcionan.
