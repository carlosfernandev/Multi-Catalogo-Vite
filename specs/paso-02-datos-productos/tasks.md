# Tareas — Paso 02

> Tareas pequeñas y atómicas: una responsabilidad por tarea, en orden de ejecución. Marcar `- [x]` al completar.

- [ ] **T1**: Actualizar `frontend/src/components/Catalogo/interfaces/iproducto.ts` con `descripcion`, `categoria`, `galeria` — *Completado cuando:* la interfaz tiene 6 campos tipados.
- [ ] **T2**: Crear `frontend/src/components/Catalogo/data/productos.ts` con `categorias` (5) y `productosMock` (8) — *Completado cuando:* el archivo importa `IProducto` y exporta ambas constantes.
- [ ] **T3**: Verificar que cada producto mock tiene `img` + 3 urls en `galeria` — *Completado cuando:* inspección visual confirma 8×3 galerías.
- [ ] **T4**: Compilar y lint — *Completado cuando:* `npm run build` y `npm run lint` pasan sin errores.
