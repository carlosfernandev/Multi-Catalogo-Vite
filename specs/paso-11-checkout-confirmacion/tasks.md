# Tareas — Paso 11

> Tareas pequeñas y atómicas: una responsabilidad por tarea, en orden de ejecución. Marcar `- [x]` al completar.

- [x] **T1**: Crear `frontend/src/components/Checkout.tsx` con interfaz `DatosEnvio` y `METODOS_PAGO` — *Completado cuando:* define tipos y lista de métodos.
- [x] **T2**: Implementar formulario de envío con validación y resumen en vivo — *Completado cuando:* campos `required` y resumen reflejan `cart`.
- [x] **T3**: Implementar `handleSubmit` con `setTimeout 1200 ms`, `clearCart` y `navigate` con `state` — *Completado cuando:* genera `MC-xxxxxx` y navega.
- [x] **T4**: Crear `frontend/src/components/Confirmacion.tsx` con `useLocation` y fallback — *Completado cuando:* lee `pedido` o muestra mensaje genérico.
- [ ] **T5**: Probar flujo completo — *Completado cuando:* checkout → confirmación muestra pedido y vacía carrito.
