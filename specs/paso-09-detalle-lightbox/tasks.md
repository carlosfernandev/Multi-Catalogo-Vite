# Tareas — Paso 09

> Tareas pequeñas y atómicas: una responsabilidad por tarea, en orden de ejecución. Marcar `- [x]` al completar.

- [ ] **T1**: Crear `frontend/src/components/DetalleProducto.tsx` con `useParams` y `getProductoById` — *Completado cuando:* carga producto por `:id` con loading.
- [ ] **T2**: Implementar galería con `imagenActiva` y miniaturas — *Completado cuando:* click en miniatura cambia imagen principal.
- [ ] **T3**: Implementar lightbox `fixed inset-0` con `‹ ›`, `×` y contador — *Completado cuando:* overlay cubre viewport y navega circular.
- [ ] **T4**: Agregar listener `Escape` con cleanup en `useEffect` — *Completado cuando:* Escape cierra lightbox sin leaks.
- [ ] **T5**: Agregar `addToCart` con feedback, estados "no encontrado" y `navigate(-1)` — *Completado cuando:* ambos flujos funcionan.
- [ ] **T6**: Probar galería y rutas inválidas — *Completado cuando:* `/producto/1` y `/producto/999` se comportan como se espera.
