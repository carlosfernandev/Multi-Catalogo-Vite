# Tareas — Paso 14

> Tareas pequeñas y atómicas: una responsabilidad por tarea, en orden de ejecución. Marcar `- [x]` al completar.

- [ ] **T1**: Reemplazar `frontend/src/components/Sidebar.tsx` con `NAV_ITEMS` y `soloAdmin` — *Completado cuando:* define 4 items con iconos SVG.
- [ ] **T2**: Filtrar `NAV_ITEMS` por `user?.rol` y resaltar activo con `useLocation` — *Completado cuando:* cliente no ve Dashboard/Mi Red y activo tiene `bg-indigo-600`.
- [ ] **T3**: Modificar `frontend/src/components/Navbar.tsx` con `user.email`, badge de rol y título por rol — *Completado cuando:* muestra correo, badge y título correcto.
- [ ] **T4**: Verificar navegación por rol y logout — *Completado cuando:* ambos perfiles ven el menú correcto y logout redirige a `/login`.
